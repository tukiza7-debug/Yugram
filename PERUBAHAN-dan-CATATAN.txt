==================================================================
 YUGRAM — PATCH PERBAIKAN (build untuk deploy Termux)
==================================================================
Berdasarkan repo: https://github.com/tukiza7-debug/Yugram (commit b51d2e1)

---------------------------------------------------------------
1. PERBAIKAN UTAMA: GAMBAR / AVATAR TIDAK MUNCUL
---------------------------------------------------------------
Penyebab: TDLib hanya mengisi `File.local.path` SETELAH file di-download.
Kode lama tidak pernah memanggil downloadFile, dan event `UpdateFile`
tidak di-handle → path selalu kosong → Coil tidak pernah load gambar.

Yang diubah:
- TDLibClientManager.kt  : tambah `fileFlow` (SharedFlow<TdApi.File>)
                           + handler `UpdateFile.CONSTRUCTOR`
                           + method `downloadFile(fileId, priority)`
- TelegramRepository.kt  : expose `fileFlow` + `downloadFile()`
- ui/components/TdImage.kt (BARU): composable yang memicu download
                           dan reaktif terhadap fileFlow.
- ChatListScreen.kt      : avatar chat → pakai TdFileImage.
- ChatRoomScreen.kt      : foto pesan & thumbnail video → TdFileImage.

---------------------------------------------------------------
2. STUB / TOMBOL MATI YANG DIPERBAIKI (sebelumnya = "coming soon")
---------------------------------------------------------------
- NewChatScreen: SEMUA tombol sekarang berfungsi
    * New Group      → NewGroupScreen (createNewBasicGroupChat)
    * New Secret Chat→ Contacts (mode pick) → createNewSecretChat
    * New Channel    → dialog input nama → createNewSupergroupChat(channel)
    * Contacts       → ContactsScreen
    * Saved Messages → buka chat diri sendiri (createPrivateChat self)
- ContactsScreen: tambah mode `onContactPick` (untuk secret chat).
- MainActivity: route Contacts pakai argumen opsional `?pick={pickMode}`.
- ChatRoomScreen: menu "Search" (palsu) dihapus → diganti "Delete Chat"
                 (dengan dialog konfirmasi, memanggil TdApi.DeleteChat).
- DataStorageSettingsScreen: tambah tombol "Clear Cache" (sebelumnya
                 hanya tampilkan angka, tidak ada tombol hapus).
- DevicesScreen: SEBELUMNYA pakai API SALAH `getConnectedWebsites()`
                 (website login via Telegram Widget) → diganti
                 `getActiveSessions()` + `terminateSession()` (perangkat
                 aktif sungguhan). Sekarang menampilkan deviceModel,
                 platform, IP, lokasi, lastActive — dan bisa di-terminate.

---------------------------------------------------------------
3. BUG LAIN YANG DIPERBAIKI
---------------------------------------------------------------
- setChatNotificationSettings: SEBELUMNYA buat ChatNotificationSettings
  kosong (semua field default 0/false) lalu set hanya muteFor → merusak
  SEMUA pengaturan notifikasi chat. Sekarang semua field diset eksplisit
  (useDefaultMuteFor, sound="default", showPreview=true, dll).

---------------------------------------------------------------
4. CATATAN / KETERBATASAN YANG MASIH ADA (belum diubah)
---------------------------------------------------------------
- Voice message: format rekaman M4A/AAC (bukan OGG/Opus standar voice
  note Telegram) — mungkin tidak dirender sebagai waveform. Belum ada
  tombol Cancel (stop = langsung kirim).
- CallScreen & CallsScreen: masih simulasi UI (tidak ada VoIP sungguhan;
  butuh libtdjni voip module).
- PrivacySettings / NotificationsSettings / Language: toggle hanya
  tersimpan di SharedPreferences lokal (belum memanggil TDLib
  setUserPrivacySettingRules / setScopeNotificationSettings / ganti
  locale aplikasi).
- ProfileScreen: tombol "Edit Profile" belum ada layar edit (tidak
  crash, hanya belum berfungsi).
- Search dalam chat: dihapus dari menu (sebelumnya palsu).

---------------------------------------------------------------
5. CARA BUILD DI TERMUX
---------------------------------------------------------------
- Butuh JDK 17 + Android SDK (platform android-36).
- Set API credentials di gradle.properties:
    TELEGRAM_API_ID=...
    TELEGRAM_API_HASH=...
- Build: ./gradlew assembleDebug
  (atau assembleRelease jika ada signing config)
==================================================================

==================================================================
 YUGRAM — NOVA RELEASE (glassmorphism redesign + fix + premium)
==================================================================

---------------------------------------------------------------
A. FIX: EDIT PROFILE (ikon pensil di Profile)
---------------------------------------------------------------
Penyebab: MainActivity memanggil ProfileScreen TANPA parameter
`onEditProfileClick`, jadi ikon pensil memakai default `{}` (kosong),
dan layar Edit Profile sendiri BELUM ADA di project.

Yang baru:
- EditProfileScreen (BARU): ubah nama depan/belakang (SetName),
  bio (SetBio), username (SetUsername), dan foto profil
  (SetProfilePhoto + InputChatPhotoStatic, via photo picker).
- Route baru "edit_profile" di MainActivity; ikon pensil, baris
  info (telepon/username/bio), dan tombol CTA semuanya membuka
  layar edit.
- Bio asli diambil via GetUserFullInfo (sebelumnya selalu kosong).

---------------------------------------------------------------
B. FIX: PESAN TERKIRIM DOBEL ("double sent")
---------------------------------------------------------------
Penyebab gabungan di ChatRoomViewModel:
1. loadMessages() lama hanya MENGGABUNGkan pesan baru (merge
   keep-old). Saat TDLib mengganti id pesan pending (temporary) ke
   id final, entri lama tetap tinggal -> teks yang sama tampil dua
   kali sebagai dua gelembung.
2. Tiga pemicu reload berbeda (UpdateNewMessage, delay 200ms
   artifisial, messageUpdateFlow) menembak sendiri-sendiri.
3. initialize() bisa dipanggil ulang sehingga collector menumpuk.

Solusi:
- Reload halaman terbaru sekarang FULL REPLACE (bukan merge),
  duplikasi mustahil; merge hanya untuk pagination (pesan lama).
- Semua pemicu masuk ke satu reloadTrigger yang di-throttle 150ms
  (satu refresh per burst event); delay 200ms dihapus.
- initialize() idempoten per chat; sendMessage() diberi debounce
  400ms anti double-tap.
- Auto-scroll kini berdasarkan id pesan TERBARU (bukan size), jadi
  tidak menghentak saat memuat pesan lama.

---------------------------------------------------------------
C. REDESIGN UI TOTAL: NOVA GLASSMORPHISM (dark premium)
---------------------------------------------------------------
- Palet baru: gradien ungu->cyan (#7B61FF -> #00D4FF), kanvas
  gelap elegan #0F0F13; semua warna Telegram blue lama di-alias
  ke identitas Nova sehingga seluruh layar ikut berganti rapi.
- Chat List: header gradien dengan wordmark "Yugram" ber-glow,
  search + more (hamburger/drawer DIHAPUS sesuai brief), search
  pill kaca terapung, kartu chat kaca dengan aksen ungu menyala
  untuk unread, badge merah-pink ber-glow, FAB gradien besar.
- Bottom navigation TERAPUNG: pill kaca blur lembut dengan glow
  ungu pada tab aktif (Chats / Calls / Settings), overlay di atas
  konten, sembunyi di layar detail.
- Settings: header gradien, kartu profil kaca besar (avatar
  berbingkai gradien), menu dalam kartu kaca ber-ikon warna-warni
  (purple, cyan, pink, green, orange, blue).
- Chat Room: gelembung outgoing ungu dalam, topbar & tombol kirim
  gradien Nova; Calls ikut gaya header gradien.
- Komponen kaca reusable di ui/components/GlassComponents.kt
  (glassSurface, NovaGradientHeader, GlassSearchBar, NovaBottomBar,
  NovaFab, GlassCard).

---------------------------------------------------------------
D. YUGRAM PREMIUM — PERCUMA SELAMANYA (layar baru "premium")
---------------------------------------------------------------
Semua fitur premium-style berikut GRATIS, tanpa langganan:
1. App Lock (PIN 4-6 digit, SHA-256, re-lock saat app keluar
   foreground) — layar kunci kaca dengan keypad.
2. Stealth Mode — buka chat TANPA mengirim read receipt
   (ViewMessages dilewati selama aktif).
3. Double-tap heart — ketuk dua kali gelembung pesan untuk reaksi
   hati (SetMessageReactions, server-side, gratis).
4. Premium star badge — bintang emas di samping nama (Settings &
   Profile).
5. Fancy text generator — 7 gaya unicode (bold/italic/script/mono/
   double/small-caps/wide) yang TIDAK ADA di Telegram resmi;
   ketuk untuk salin.
6. Scheduled sending — tekan-lama tombol kirim di chat untuk
   menjadwalkan 1/8/24 jam (MessageSchedulingStateSendAtDate,
   server-side).

Catatan jujur: fitur premium Telegram yang ditegakkan di SERVER
(unlock emoji premium, upload 4GB, badge server, dll.) tidak bisa
dibuka dari sisi klien — yang kami berikan adalah fitur premium-
style yang benar-benar berfungsi dan bonus unik di atas itu.

==================================================================
 PATCH: FITUR NEKOGRAM + MUAT TURUN PUKAL (all / selected / bulk)
==================================================================

---------------------------------------------------------------
1. MUAT TURUN PUKAL MEDIA (download all / selected / bulk)
---------------------------------------------------------------
- core/download/MediaDownloadManager.kt (BARU): enjin muat turun pukal
    * barisan (queue) dengan paralelisme terkawal 1-8 sambungan
    * kemajuan langsung (progress) setiap fail + keseluruhan
    * batal satu fail / semua; kosongkan selesai
    * fail siap DISIMPAN AUTOMATIK ke galeri (Pictures/Yugram,
      Movies/Yugram) melalui core/util/GallerySaver.kt (BARU)
    * tetapan TDLib `connections_for_media_download` diselaraskan
- ui/components/DownloadProgressPanel.kt (BARU): panel terapung
    paparan kemajuan (boleh dikembang, batal per-fail)
- ChatRoomScreen:
    * menu (tiga titik) -> "Muat turun semua media" (SearchChatMessages
      filter PhotoAndVideo + Document, hingga 700 fail)
    * pemilihan mesej -> toolbar muat turun (hanya media terpilih)

---------------------------------------------------------------
2. FITUR NEKOGRAM YANG DITAMBAH
---------------------------------------------------------------
- Pemilihan mesej: tekan lama bubble -> menu konteks "Pilih";
  toolbar pemilihan: Pilih Semua / Muat Turun / Maju / Salin / Padam
- Terjemahan mesej (TDLib native TranslateText, tiada API luar):
    * menu konteks "Terjemah" + auto-terjemah mesej masuk (toggle)
    * bahasa sasaran boleh dipilih (ms, id, en, zh, ar, es, fr, ja,
      ko, ru, th, vi) — cache per-mesej dalam sesi
- Salin teks mesej (satu / pukal) ke papan keratan
- Majukan mesej ke mana-mana chat terkini + toggle
  "Tanpa nama pengirim" (sendCopy / hide quote, gaya Nekogram)
- Sahkan sebelum hantar (toggle)
- Hantar senyap (disableNotification) (toggle)
- Butiran mesej (ID, jenis, tarikh, pengirim) melalui menu konteks
- Padam mesej terpilih (revoke = padam untuk semua orang)
- ui/settings/NekogramSettingsScreen.kt (BARU): layar tetapan
  "Fitur Nekogram" dijana dari kad menu Settings ("Lainnya")

---------------------------------------------------------------
3. KEMAS KINIAN STRUKTUR
---------------------------------------------------------------
- AppSettingsManager: +6 tetapan neko_* (confirm/silent/translate/
  lang/connections/forwardNoQuote)
- TelegramRepository: +forwardMessages, translateText, deleteMessages,
  searchChatMedia, setOptionInt, cancelDownloadFile,
  sendTextMessageWithOptions
- MediaViewer: delegasi simpan galeri ke GallerySaver (satu sumber)
- ChatRoomViewModel: keadaan pemilihan, cache terjemahan, auto-terjemah,
  tindakan pukal
- Semua signature TDLib disahkan terhadap TdApi.java tdlibx 1.8.56
- verify_nekogram.py: 65 semakan statik (baki brace, import, ikon,
  TdApi, wiring, emoji) — SEMUA LULUS sebelum push
